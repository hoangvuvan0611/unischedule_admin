import type { Metadata } from "next";import { Geist, Geist_Mono } from "next/font/google";import "./globals.css";import MySidebar from "@/components/layout/mySidebar";import {SidebarProvider} from "@/contexts/layoutContext";import MyNavbar from "@/components/layout/myNavbar";const geistSans = Geist({  variable: "--font-geist-sans",  subsets: ["latin"],});const geistMono = Geist_Mono({  variable: "--font-geist-mono",  subsets: ["latin"],});export const metadata: Metadata = {  title: "UniSchedule",  description: "Generated by create next app",  icons: {    icon: [      { url: "/logo_unischedule.jpg", type: "image/jpg", sizes: "192x192" },    ],  }};export default function RootLayout({  children,}: Readonly<{  children: React.ReactNode;}>) {  return (    <html lang="en">      <body        className={`${geistSans.variable} ${geistMono.variable} antialiased`}      >        <div className="min-h-screenbg-zinc-50 font-sans dark:bg-black max-h-screenbg-zinc-50">          <SidebarProvider>            {/*navbar*/}            <div className="">              <MyNavbar/>            </div>            <div className="flex justify-around">              {/*Sidebar danh sach cac tab*/}              <div className="w-2/12 border-r-gray-300 border-r-1 border-dashed">                <MySidebar/>              </div>              <div className="w-10/12 bg-blue-50">                {children}              </div>            </div>          </SidebarProvider>        </div>      </body>    </html>  );}